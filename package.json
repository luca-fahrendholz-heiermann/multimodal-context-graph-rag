{
  "name": "multimodal-graph-rag-cross-project-context-modeling",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "docker compose up --build",
    "start": "docker compose up",
    "build": "echo 'Build is handled via docker compose images.'",
    "lint": "echo 'Lint placeholder: TypeScript strict lint is not yet enforced in this demo repository.'",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "express": "^4.21.2"
  }
}
